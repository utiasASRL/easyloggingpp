# - Config file for the robochunk package
# It defines the following variables
# easyloggingpp_INCLUDE_DIRS - include directories for the robochunk libraries

# Compute Paths
get_filename_component(EASYLOGGINGPP_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(easyloggingpp_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
message(STATUS "Found easyloggingpp")
message(STATUS "   Includes: ${easyloggingpp_INCLUDE_DIRS}")
